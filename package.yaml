name:                LinkScraper
version:             0.1.0.0
github:              "githubuser/LinkScraper"
license:             BSD3
author:              "Author name here"
maintainer:          "example@example.com"
copyright:           "2019 Author name here"

extra-source-files:
- README.md
- ChangeLog.md

# Metadata used when publishing your package
# synopsis:            Short description of your package
# category:            Web

# To avoid duplicated efforts in documentation and dealing with the
# complications of embedding Haddock markup inside cabal files, it is
# common to point users to the README.md file.
description:         Please see the README on GitHub at <https://github.com/randomthought/LinkScraper#readme>

dependencies:
- base >= 4.7 && < 5
- stm
- containers
- bytestring
- string-class
- tagsoup
- monad-loops
- curl == 1.3.8
- text
- hxt
- hxt-curl
- HandsomeSoup
- optparse-applicative
# - pretty-simple
- mtl
# - bloomfilter
- lens
- yaml
- aeson
- transformers-base
- unliftio
- unliftio-core
- monad-control
- easy-file
- regex-tdfa
- tld

library:
  source-dirs: src

executables:
  LinkScraper-exe:
    main:                Main.hs
    source-dirs:         app
    ghc-options:
    - -threaded
    - -rtsopts
    - -with-rtsopts=-N
    dependencies:
    - LinkScraper

tests:
  LinkScraper-test:
    main:                Spec.hs
    source-dirs:         test
    ghc-options:
    - -threaded
    - -rtsopts
    - -with-rtsopts=-N
    dependencies:
    - LinkScraper
